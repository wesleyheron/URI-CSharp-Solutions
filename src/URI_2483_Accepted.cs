using System; 

class URI {

    static void Main(string[] args) { 

        int i = int.Parse(Console.ReadLine());
            char[] arr = new char[i];
            string a = "";
            for (int j = 0; j < i; j++)
            {
                a += "a";
            }

            Console.WriteLine("Feliz nat{0}l!",a);

    }

}
